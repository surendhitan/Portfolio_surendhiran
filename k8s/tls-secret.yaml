# ─── TLS Secret ──────────────────────────────────────
# Created by: kubectl create secret tls portfolio-tls-secret \
#   --cert=ssl/surendhiran.dev.com.crt \
#   --key=ssl/surendhiran.dev.com.key \
#   --namespace=portfolio
#
# NOTE: This file is a TEMPLATE. The actual secret is created
# by the run-k8s.ps1 script using kubectl to avoid storing
# base64-encoded keys in plaintext YAML.
apiVersion: v1
kind: Secret
metadata:
  name: portfolio-tls-secret
  namespace: portfolio
  labels:
    app: portfolio
    type: tls
type: kubernetes.io/tls
data:
  # Populated at deploy time by run-k8s.ps1 via:
  # kubectl create secret tls portfolio-tls-secret --cert --key
  tls.crt: ""
  tls.key: ""
